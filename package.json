{
  "name": "bucket-o-slop",
  "version": "0.1.0",
  "private": true,
  "description": "Integration test target for slop-mop — poly-language mono-repo of slop examples",
  "scripts": {
    "test": "echo \"See js-tests/ for test fixtures\""
  }
}
